# Vercel / Next.js dashboard
# Point the UI to a committed or deployed report JSON path.
REPORT_JSON_PATH=artifacts/altersense/operator_report_cam33_phase_mixed_station136_plus5.json

# Local GPU worker only
# Do not place these in Vercel unless you intentionally expose local processing there.
PYTHON_BIN=python3
GOOGLE_API_KEY=your_gemini_api_key_here
